import React,{useState} from 'react';import{createRoot}from'react-dom/client';import'./style.css';
const API=import.meta.env.VITE_API_BASE_URL||'';
function App(){const [file,setFile]=useState(null),[status,setStatus]=useState('Ready');const upload=async()=>{if(!file)return;setStatus('Uploading…');const f=new FormData();f.append('file',file);const r=await fetch(`${API}/v5/process-image`,{method:'POST',body:f});setStatus(r.ok?`Queued: ${(await r.json()).job_id}`:`Error ${r.status}`)};return <main><header><h1>ChromaCore v5</h1><p>AI Media Processing Platform</p></header><section><h2>Image Processing</h2><input type="file" accept="image/*" onChange={e=>setFile(e.target.files?.[0]||null)}/><button onClick={upload} disabled={!file}>Process Image</button><p>{status}</p></section><section><h2>System</h2><p>API: {API||'same-origin / configure VITE_API_BASE_URL'}</p></section></main>};createRoot(document.getElementById('root')).render(<App/>);